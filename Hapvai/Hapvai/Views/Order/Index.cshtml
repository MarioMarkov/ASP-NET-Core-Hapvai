@model OrderViewModel
@{
    ViewData["Title"] = "Order";
}

<style>
    #map {
        height: 400px;
        /* The height is 400 pixels */
        width: 100%;
        /* The width is the width of the web page */
    }
</style>

<h1>My Order</h1>
@if (Model == null)
{


    <span>No Items here</span>
}
else
{
    <h2>Order ID : @Model.OrderId</h2>


    <div>

        @foreach (var oi in Model.OrderItems)
        {
            <div>
                <img src="@oi.Product.ImageUrl" alt="Alternate Text" width="100px" height="100px" />
                <span>@oi.Product.Name</span> <span>Price: @oi.Product.Price $</span>
                <span>Quantity: @oi.Quantity</span>
            </div>
        }

        <div><span>Total: @Model.OrderItems.Sum(oi => oi.Product.Price) $</span></div>


        <!--<html>
        <head>
            <title>Add Map</title>
            <script src="https://polyfill.io/v3/polyfill.min.js?features=default"></script>

            <link rel="stylesheet" type="text/css" href="./style.css" />
            <script type="module" src="./index.js"></script>
        </head>
        <body>
            <h3>My Google Maps Demo</h3>-->
            <!--The div element for the map -->
            <!--<div id="map"></div>


        </body>
    </html>-->

    <button>Make Order</button>


</div>

}
@*<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAp2b829f5OURiC52-GJA0t2f2PxL0y6E4&callback=initMap&v=weekly"
        defer></script>
<script>
    // Initialize and add the map
    function initMap() {
        // The location of Uluru
        const uluru = { lat: -25.344, lng: 131.031 };
        // The map, centered at Uluru
        const map = new google.maps.Map(document.getElementById("map"), {
            zoom: 4,
            center: uluru,
        });
        // The marker, positioned at Uluru
        const marker = new google.maps.Marker({
            position: uluru,
            map: map,
        });
    }

    window.initMap = initMap;
</script>*@